- restore CORRECT state after power failure. Need to re-read thermostats, and react on its state properly. To do so, we need some flag in SPI_loop not to check for _changed , but only to _pressed in_pins.